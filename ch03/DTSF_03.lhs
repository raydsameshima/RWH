DTSF_03.lhs

Defining Types, Streamlining Functions

> module DTSF_03 where

on page 47

There is no hard and fast rule for deciding when it's better to use a tuple or a distinct data type, but here's a rule of thumb.
If we are using compound values widely in our code (as almost all nontrivial programs do), adding data declarations will benefit us in both type safety and readability.


